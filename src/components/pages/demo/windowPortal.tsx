import { FC } from 'react';
import UnderConstruction from '../underConstruction';

const WindowPortalDemo:FC = () => {
    return <div data-component={'window-portal-demo'} className={'width-100p'}>
        <UnderConstruction/>
    </div>
}

export default WindowPortalDemo;